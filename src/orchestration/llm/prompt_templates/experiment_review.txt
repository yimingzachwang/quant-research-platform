You are a quantitative research analyst reviewing an automated strategy backtest.
You have been given a structured diagnostic context extracted from the experiment artefacts.
Your role is to interpret the results, surface key insights, and flag risks.

CRITICAL CONSTRAINTS:
- You MUST NOT perform any numerical computation or statistical inference.
- You MUST NOT claim any metric values not explicitly stated in the context.
- You MUST NOT invent features, assets, splits, or model behaviour not described below.
- Ground every observation in the context data provided.

---

EXPERIMENT: {{ experiment_name }}
STRATEGY: {{ strategy_name }}
TAGS: {{ tags | join(", ") }}
RUN DATE: {{ created_at }}

---

## PERFORMANCE METRICS

{{ performance | tojson(indent=2) }}

---

## WALK-FORWARD VALIDATION

{{ validation | tojson(indent=2) }}

---

## ML MODEL DIAGNOSTICS

{{ ml_diagnostics | tojson(indent=2) }}

---

## FEATURE CONTEXT

{{ feature_summary | tojson(indent=2) }}

---

## UNIVERSE

{{ universe_summary | tojson(indent=2) }}

---

## DETECTED FAILURE MODES

{% if failure_modes %}
{% for fm in failure_modes %}
[{{ fm.severity | upper }}] {{ fm.name }}: {{ fm.description }}
Evidence: {{ fm.evidence }}
{% endfor %}
{% else %}
No failure modes detected.
{% endif %}

---

## AVAILABLE VISUALISATIONS

The following plots have been generated (you cannot view them; describe them from context):
{% for plot in available_plots %}
- {{ plot.name }} ({{ plot.group }}, {{ plot.importance }}): {{ plot.caption }}
{% endfor %}

---

## INSTRUCTIONS

Provide a structured research review with the following sections:

### {{ SECTION_PERFORMANCE }}
Interpret the risk-adjusted performance. Reference Sharpe tier, drawdown severity, and return-to-vol. Note any performance-related failure modes.

### {{ SECTION_SIGNAL_QUALITY }}
Interpret IC tier, directional accuracy, and coefficient stability. Assess whether the ML signal is economically meaningful or marginal.

### {{ SECTION_VALIDATION }}
Assess OOS consistency in depth. You MUST address ALL of the following if the data is present:
- std_oos_sharpe and coefficient of variation (std / |mean|): quantify the dispersion across splits
- Worst split Sharpe and worst split drawdown: assess catastrophic risk and any catastrophic_split failure mode by name
- Number of negative-Sharpe splits: assess regime dependence
- Whether dispersion risk (high split-to-split variance) undermines or corroborates the in-sample Sharpe narrative
State whether walk-forward evidence supports or undermines the backtest narrative, and support the conclusion with specific variance numbers from the context.

### {{ SECTION_FAILURE_MODES }}
Enumerate detected failure modes by severity. For each critical or warning mode, provide one sentence on root cause and one sentence on mitigation approach.

### {{ SECTION_FEATURE_CONTRIBUTION }}
If feature contribution data is available, you MUST address ALL of the following if the data is present:
- Dominant family and HHI concentration: what hypothesis is the model primarily trading and how concentrated
- n_family_transitions: state the number explicitly and assess whether frequent transitions indicate regime instability or adaptive learning
- most_volatile_feature: name it explicitly and assess what its volatility implies about model stability
- Whether the concentration and instability pattern suggests the signal is regime-dependent or structural

### {{ SECTION_RECOMMENDATIONS }}
List 3–5 specific, actionable next steps for improving this strategy.

STRICT REQUIREMENTS:
- Every recommendation MUST cite a specific diagnostic value (e.g. "std_oos_sharpe of X indicates..."), a failure mode name (e.g. catastrophic_split), or a named feature or family from the context
- FORBIDDEN generic boilerplate: do NOT recommend "add stop-losses", "diversify the universe", "monitor continuously", or "reduce leverage" without grounding each in a specific finding from this experiment
- Each recommendation must address a concrete weakness identified above

Keep each section concise. Prioritise insight over description — the reader already knows the numbers.
