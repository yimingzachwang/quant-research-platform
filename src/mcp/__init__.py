"""Zeto MCP interface package.

Exposes the existing governed Research API as MCP tools so an external chat
client (e.g. LM Studio) can drive the AI-assisted research workflow.  This is an
interface layer only — it adds no new orchestration behaviour and wraps the
Research API exclusively.
"""
