# Features

Feature engineering must be deterministic, leakage-aware, and explicitly aligned by timestamp and symbol.

Feature transforms should declare lookback windows, required fields, and missing data policies.
