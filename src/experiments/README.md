# Experiments

Experiments provide the config-driven workflow for reproducible research.

`ExperimentRunner` coordinates configured runs. `ExperimentTracker` provides the MLflow hook, with no-op behavior by default.
