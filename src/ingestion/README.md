# Ingestion

Ingestion jobs move external or raw data into validated research datasets.

The scaffold includes no-op implementations only. Real ingestion must add schema validation, lineage, and freshness checks.
