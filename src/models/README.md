# Models

The model layer owns training, validation schemes, model artifacts, and inference contracts.

Model implementations should log parameters, metrics, and artifacts through experiment tracking when enabled.
