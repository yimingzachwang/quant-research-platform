# Configs

Configuration is the primary control plane for research. Experiments should compose smaller config files for universes, data, features, models, portfolio rules, risk checks, backtests, reports, and tracking.

Do not place secrets in configuration. Use environment variables or a secret manager for credentials.
