# Docker

Docker files provide reproducible local and CI execution. The default image installs the package with research and development extras and runs `pytest`.

Use `docker compose up mlflow` when a local MLflow tracking server is useful.
